# YearCal Web アプリケーション 仕様書

## 1. 概要

年間カレンダーWebアプリ。1画面で12ヶ月を俯瞰し、月ごとに予定（イベント）を色付きで登録・管理できる。データはサーバに永続化し、GitHubで公開してブラウザからアクセス可能にする。

---

## 2. 機能要件

### 2.1 カレンダー表示
- 1画面に12ヶ月分を表示（3列 × 4行のグリッドレイアウト）
- 表示年を切り替え可能（前年 / 翌年ボタン）
- 各月セルには月名（1月〜12月）とその月の予定一覧を表示

### 2.2 予定管理
| 操作 | 内容 |
|------|------|
| 追加 | 月セルをクリック → モーダルで予定を入力 |
| 編集 | 予定をクリック → モーダルで内容を変更 |
| 削除 | 予定の削除ボタンから削除 |

**予定データ項目**
- タイトル（必須、最大50文字）
- 日付（年・月・日）
- メモ（任意、最大200文字）
- 色（6色以上のプリセットから選択）

### 2.3 色設定
プリセット色（最低6色）をパレットから選択。例：
- 赤 `#FF5252`
- オレンジ `#FF9800`
- 黄 `#FFC107`
- 緑 `#4CAF50`
- 青 `#2196F3`
- 紫 `#9C27B0`

---

## 3. 画面仕様

### 3.1 メイン画面（年間カレンダー）

```
┌─────────────────────────────────────────────────┐
│  ← 2025年  →                                    │
├─────────────┬─────────────┬─────────────────────┤
│    1月      │    2月      │    3月              │
│  ・予定A    │  ・予定B    │                     │
├─────────────┼─────────────┼─────────────────────┤
│    4月      │    5月      │    6月              │
│             │  ・予定C    │  ・予定D            │
├─────────────┼─────────────┼─────────────────────┤
│    7月      │    8月      │    9月              │
│             │             │                     │
├─────────────┼─────────────┼─────────────────────┤
│   10月      │   11月      │   12月              │
│  ・予定E    │             │  ・予定F            │
└─────────────┴─────────────┴─────────────────────┘
```

- 各月セルの予定はタイトルと色バッジで表示
- 予定が多い場合は「+N件」で省略表示、クリックで全件展開

### 3.2 予定入力モーダル

```
┌──────────────────────────────┐
│  予定を追加（3月）           │
├──────────────────────────────┤
│  タイトル: [_______________] │
│  日付:     [年][月][日]      │
│  メモ:     [_______________] │
│            [_______________] │
│  色:       ● ● ● ● ● ●    │
├──────────────────────────────┤
│         [キャンセル] [保存]  │
└──────────────────────────────┘
```

---

## 4. データ設計

### 4.1 イベントデータモデル

```json
{
  "id": "uuid-v4",
  "year": 2025,
  "month": 3,
  "day": 15,
  "title": "花見",
  "memo": "公園に集合",
  "color": "#4CAF50",
  "createdAt": "2025-01-10T12:00:00Z",
  "updatedAt": "2025-01-10T12:00:00Z"
}
```

### 4.2 API エンドポイント

| メソッド | パス | 説明 |
|----------|------|------|
| GET | `/api/events?year=2025` | 指定年のイベント一覧取得 |
| POST | `/api/events` | イベント新規作成 |
| PUT | `/api/events/:id` | イベント更新 |
| DELETE | `/api/events/:id` | イベント削除 |

---

## 5. 技術スタック

| レイヤー | 採用技術 | 理由 |
|----------|----------|------|
| フロントエンド | React + TypeScript | コンポーネント管理・型安全 |
| スタイル | Tailwind CSS | 迅速なレイアウト構築 |
| バックエンド | Node.js + Express | 軽量RESTful API |
| データベース | SQLite（開発） / PostgreSQL（本番） | シンプルな永続化 |
| ホスティング（フロント） | GitHub Pages | 無料・GitHub連携 |
| ホスティング（バックエンド） | Railway / Render（無料枠） | 手軽なサーバデプロイ |
| バージョン管理 | GitHub | 公開リポジトリ |

---

## 6. システム構成

```
ブラウザ
  │  HTTP(S)
  ▼
GitHub Pages（静的ファイル: React SPA）
  │  REST API (HTTPS)
  ▼
Railway / Render（Node.js + Express）
  │
  ▼
PostgreSQL（イベントデータ）
```

---

## 7. 非機能要件

| 項目 | 要件 |
|------|------|
| レスポンシブ | デスクトップ（1024px以上）を主対象 |
| ブラウザ対応 | Chrome / Edge / Safari 最新版 |
| パフォーマンス | 初期ロード 3秒以内 |
| セキュリティ | CORS設定、入力バリデーション（XSS対策） |
| 認証 | 初期版はなし（シングルユーザ想定） |

---

## 8. 開発フェーズ

| フェーズ | 内容 | 成果物 |
|----------|------|--------|
| Phase 1 | フロントエンドUI実装（静的モック） | 年間カレンダー表示・モーダルUI |
| Phase 2 | バックエンドAPI実装 | REST API + DB |
| Phase 3 | フロント↔バックエンド接続 | 動作するフルスタックアプリ |
| Phase 4 | GitHub公開・デプロイ | 公開URL |

---

## 9. ディレクトリ構成（予定）

```
yearcal-web/
├── frontend/            # React アプリ
│   ├── src/
│   │   ├── components/
│   │   │   ├── YearCalendar.tsx   # 年間グリッド
│   │   │   ├── MonthCell.tsx      # 月セル
│   │   │   ├── EventBadge.tsx     # 予定バッジ
│   │   │   └── EventModal.tsx     # 入力モーダル
│   │   ├── hooks/
│   │   │   └── useEvents.ts       # APIフック
│   │   └── App.tsx
│   └── package.json
├── backend/             # Express API
│   ├── src/
│   │   ├── routes/events.ts
│   │   ├── models/Event.ts
│   │   └── index.ts
│   └── package.json
└── README.md
```

---

## 10. 未決事項・今後の拡張候補

- [ ] 認証機能（複数ユーザ対応）
- [ ] 予定の繰り返し設定（毎年など）
- [ ] カレンダーのエクスポート（PDF / iCal）
- [ ] 祝日の自動表示（日本の祝日API連携）
- [ ] ドラッグ&ドロップで月をまたいだ予定移動
